# import pprint
from pprint import pprint


realistic_dict = {
    "hello": ["a greeting of welcome", "the first thing we say in a programming language"],
    "world": ["a planet"],
    "python": ["a snake"]
}

# pprint.pprint(realistic_dict)
pprint(realistic_dict, indent=7)
